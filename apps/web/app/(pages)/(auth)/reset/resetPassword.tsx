import ResetForm from 'apps/web/app/components/organisms/ResetForm';

/* eslint-disable-next-line */
export interface ResetPageProps {}

export default function ResetPage(props: ResetPageProps) {
	return (
		<main className='container'>
			<ResetForm />
		</main>
	);
}
